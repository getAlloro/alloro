# Inversion Engine, Build Handoff for Dave (bundled into the product repo, 2026-07-07)

Everything Dave's Claude needs to build the inversion engine, delivered INTO the repo you already pull, because the #1 past-handoff failure was docs Dave couldn't open (a personal-repo link he had no access to).

**START HERE:** `DAVE-CLAUDE-HANDOFF.md`.

**Layout (paths match the handoff's references):**
- `specs/` , the 7 chapter specs (inversion-01..07) + `inversion-map.md`.
- `strategy/` , the frame-validation guide (the FLIP + built/unbuilt receipts) + `canon-to-stake.md` (the Value #2 stake).
- `research/` , the lever-outcome evidence map (the honesty guardrails on lever claims).
- `library/canon/` , the ICP spine (who the customer is).

**The code is already on THIS repo:** Slice 1 = PR #145 (branch `claude/slice-1-data-truth`); the honesty harness = branch `claude/engine-honesty-harness`.

**NOT bundled, by design (internal / personal tier):** the team profiles and the personal layer of the mission. They ground how we PREPARE handoffs, but they are not build-facing and not Dave's to hold.

Coherence-pass clean; Tier-1 blockers resolved; the wiring is Dave's.
