# Inversion Engine, Build Handoff for Dave (bundled into the product repo, 2026-07-07)

Everything Dave's Claude needs to build the inversion engine, delivered INTO the repo you already pull (the #1 past-handoff failure was docs Dave couldn't open).

**START HERE:** `DAVE-CLAUDE-HANDOFF.md`.

**Layout:**
- `specs/` , the 7 chapter specs (inversion-01..07) + `inversion-map.md`.
- `strategy/` , the frame-validation guide, the Value-#2 stake, and the MISSION (the WHY).
- `research/` , the lever-outcome evidence map (honesty guardrails).
- `library/canon/` , the ICP spine (who the customer is).
- `profiles/` , how each of us works (Dave, Corey, Jo), so the team works better together.

**The code is already on THIS repo:** Slice 1 = PR #145 (`claude/slice-1-data-truth`); the honesty harness = `claude/engine-honesty-harness`.

**SHARING (Corey, 2026-07-07):** everything here is INTERNAL TEAM VALUE, share it freely with the team, that is how we grow as a collective. The ONLY guardrail is PUBLIC: Corey's personal story (in the mission + his profile) is shareable INTERNALLY but NEVER goes into public content or customer-facing copy, and public-facing work always keeps the OWNER as the hero (Alloro is the guide). Internal = open; public = restricted.

Coherence-pass clean; Tier-1 blockers resolved; the wiring is Dave's.
