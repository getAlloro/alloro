# Operator scaling playbooks, Martell + Hormozi (brain-sweep 2026-07-08)

*Two YouTube transcripts Corey dropped: Dan Martell's 12-step "scale so fast it feels like cheating" + Alex Hormozi's growth-levers ("grew so fast it felt illegal", $1-2M/mo in year one). Weight: MED, operator/scaling doctrine for how WE grow Alloro (not Alloro-product ICP ore). ~70% corroborates existing homes (mapped below). External = 2 creator videos, treat as heuristics not evidence.*

## The one insight both share (the compounding thesis)
Optimizations are CAPPED (a close rate or opt-in rate can only reach 100%); SYSTEMS + new levers (a "boom" = an order-of-magnitude change like advertising; buying back time; retention/LTV) COMPOUND. The enemy of compounding = staying in the weeds optimizing what's capped / doing $10 tasks. Build a SYSTEM, not a plan.

## Additive bits worth keeping (sharper than what we have)
- **Buyback-rate math (Martell):** time-value = (annual income / 2000) / 4. Anything below that rate = delegate/systemize. Two-week 15-min time audit; highlight energy-drains red; reinvest freed time UP the leverage ladder. Sharpens DOCTRINE's economy-of-force + the ON-not-IN goal.
- **The "$X to fix it" autonomy tiers (Martell):** anyone who sees a bottleneck may spend to fix it without asking, tiered $50 IC / $500 leader / $5k manager / $50k C-suite; only ask = tell the leader after. A concrete dollar-sharpening of DOCTRINE's Type-2-door / Grove-TRM delegation-depth (maps the Green lane to a number).
- **System-not-plan build (Martell):** camcorder (record yourself doing the activity) -> course (document steps) -> cadence (weekly schedule) -> checklist (7-10 daily things that produce the result). Turns a one-off into a delegatable SYSTEM. This IS "close a box that leaves an asset."
- **48-hour quick-win retention (Martell):** deliver the customer's whole investment's worth of result in the first 48h -> churn 10% to 3% -> ~4x LTV. Time-boxes the Peloton first-win (`owner-com`, `local-owner-wants`).
- **Hormozi's levers:** obscurity is the #1 enemy under $1M (first 4 hrs/day = "core four for more": outreach/content/ads, pick ONE, all-in); "boom" (advertising 100x vs optimization's 100% cap); clear-not-clever; proof-over-promise; more-better-new; word-of-mouth; the list.

## Already homed (dedup, corroborates, do NOT re-migrate)
- Constraint-first = DOCTRINE Musk-ordering/jidoka + `strategy/acquisition-mechanism-model` diagnostic law ("treat the rate-limiting failure").
- Proof-over-promise / clear-not-clever = Value #6 + `self-proving-loop` + The Standard plain-language (`operating-doctrine`).
- Obscurity-is-the-enemy = the Attraction thesis; word-of-mouth = the advocacy loop (`acquisition-mechanism-model`).
- Zone-of-genius / replace-yourself = the ON-not-IN / amplify-people frame.

## ⚠️ Guardrail
Martell's offer step uses a **money-back guarantee** as risk-reversal, that VIOLATES Value #6 (no guarantees). Take the transformation-offer + urgency/scarcity mechanism; DROP the guarantee. Same trap as the Objection-Library flag (`canon-to-stake` #4).
