# Operating Doctrine, message + delivery method (migrated from Notion 2026-07-08)

*Durable METHOD migrated from the Notion doctrine docs (The Standard, Hero Arc Substrate, Command the Message, Offer & Money Model). The CORE frames already live elsewhere, owner=hero + Cesar in `library/canon/what-alloro-is-and-isnt` + `mvv`; command-the-field + Reactive in `library/lattices/knowledge-lattice`. This file homes the ADDITIVE method specifics + the flags; it does NOT re-state the core. ⚠️ Two contradictions to reconcile are flagged inline.*

## 1. The Standard, scored (not just stated)
Meta-question ("does it make a human feel understood BEFORE informed?") = 40% of a 0-100 composite; 9 weighted sub-criteria = 60%: Recognition Test (would the owner's spouse recognize them in this copy?), Patient-Voice Match (only words from real reviews, never invented), Recipe Compliance (one finding + one dollar figure + one action), Cesar Millan (Alloro translates, the owner is the hero), Plain-English Mom Test (a 9th-grader acts in 10s; a banned-jargon list), Provenance (no source link, no ship), Never-Blank / No-Future-Promises (thin data -> a gap statement, not fabrication; a below-confidence number -> OMIT it = the Theranos guardrail), Public-Safe (HIPAA/CAN-SPAM), Fear-Acknowledged-Before-Service.
- **The tri-score insight:** SEO + AEO + CRO are three SYMPTOMS of ONE root cause (stock/template content) with ONE root cure (authentic specificity from actually knowing the owner). Score high on The Standard and you win all three at once; retrofitting each layer later costs far more. Build it in from the first page.

## 2. Hero Arc, the customer-as-protagonist method (SUBORDINATE to the honesty gate)
Two paired, both-mandatory principles: **Show the Work** (labor visible; every artifact is proof; Labor-Illusion ~22% perceived-value lift; produces Trust; prevents churn-by-invisibility) + **Crown the Hero** (customer-as-protagonist; produces Pride; prevents commoditization-by-AI-tools). The Six Elements every surface inherits: (1) Voice = first-person, time-stamped, warm ("I caught", not "your dashboard shows"); (2) Arc = past/present/future, customer as protagonist; (3) Stakes = a named dollar figure, a NAMED rival, time pressure; (4) Hero Moments = five named celebrations (First Month, First Comeback, Defended Position, Year Mark, Milestone); (5) Awe Vector = "while you slept" guardianship; (6) the Reactive layer (the moat) = Reportive ("here's what I saw") + Reactive ("here's what I DID about it, honest after-report").
- **Reactive-rollout doctrine:** every default-to-action surface starts default-to-APPROVE for 30 days per customer; after a stable approval pattern it flips to default-to-action for that customer ("trust earns the trust"). Never auto-act on Red-blast (billing, customer data, irreversible negative-review posts).
- **⚠️ HONESTY SUBORDINATION (flag):** the dramatized examples ("$7,500 at risk", "3:14 AM Tuesday") are ONLY honest if backed by real, provenanced data. This Substrate sits UNDER The Standard's Provenance + No-Future-Promises + the 2026-07 data-truth work (today's inversion build). Migrate the method; keep the copy subordinate to the honesty gate, otherwise "make them feel something" becomes fabricated drama.

## 3. Command the Message, command the field before engaging it
Every touchpoint is a CONFIRMATION, not an introduction (the message/frame/context reached the audience first: Brady's pre-snap audible, Jobs defining the category, Sun Tzu "won before fought"). The 5-step sequence, no step skipped: Frame established -> Awareness -> Curiosity activated -> Proof delivered (something specific and true about THEIR practice) -> Decision offered. The GLP-1 model: the mechanism is invisible, the RESULT is the product, the owner needs only "what stands between me and the outcome, and the one action that removes it."
- **⚠️ CANON CONTRADICTION (flag, Corey to reconcile):** the source page hard-asserts "more new patients is the ONLY stated outcome that matters, every output traces to it." That contradicts Value #6 (no guarantees / no outcome-promises) AND The Standard's own No-Future-Promises. Route the FIELD-CONTROL + outcome-backward-reasoning principle in; do NOT home the "promise more new patients" language. (Source dated March 2026, pre-dates the current honesty posture.)

## 4. Selling under the no-guarantee rule (Offer & Money Model)
- Win PERCEIVED LIKELIHOOD with proof-in-hand (the live audit; for existing clients, their own results), never a promise. Never "you will."
- "Designed-to" intention language REPLACES the guarantee: "built to make your practice the obvious choice when someone searches for care, and to show you in plain numbers where patients and referrals come from", never "in 90 days you'll rank" or "or we work free."
- Ownership stated as plain FACT, not a risk-reversal pitch: you own the site, data, domain; leave anytime with all of it.
- Annual prepay = per-account founder discretion, NEVER productized/advertised, no "save $X / lock in", no countdown/urgency ("$2,000 is the discount; there is no negotiating down"). Offer only where value is already felt (earn-before-asking, Value #5).

## 5. The claim boundary, autonomy = ingestion only (from Product Ground Truth, #17 sweep)
The seller/claim discipline (reinforces the built-vs-unbuilt guardrail): honesty over optimism, never claim anything mocked, dev-only, or unapproved as live.
- **AI-drafts-humans-stake is enforced in CODE, not just policy:** a fail-closed interceptor means nothing publishes as the practice or messages a patient without a human approve. There is NO SMS and NO nurture / review-request auto-sender at all.
- **The ONLY autonomous-and-scheduled behavior is data INGESTION** (daily review/post sync). Everything on the content / SEO / GBP surface is human-triggered-per-request or absent. Do NOT claim: autonomous content refresh, scheduled schema/SEO upkeep, sitemap/IndexNow, `llms.txt`, or any GEO/AEO automation.

## 6. The output Recipe + the 10pm test (from The Alloro Recipe, #17 sweep)
(The core "one finding + one dollar + one action" is already scored in §1 as Recipe Compliance; these are the additive specifics.)
- Every output does exactly TWO things: (1) name the specific truth the owner couldn't see, tied to a core need (safety / status / belonging / purpose); (2) attach the economic consequence at 30 / 90 / 365 days. Both = intelligence; one = mere data.
- **The 10pm test:** would a tired, skeptical owner at 10pm understand it immediately and know the next move? If not, the fix is always MORE SPECIFIC, never more explanation.
- **⚠️ Frame caution (not a canon violation):** The Recipe's own category label ("Business Clarity / a scoreboard / the truth you can't see before you pay") pre-dates the locked attraction-engine lane (first-impression -> booked appointment, DFY execution). Migrate the discipline; do NOT let the "clarity-scoreboard" framing overwrite the current attraction-lane canon.

## 7. The three-anchor Gate (from The Alloro Gate, #17 sweep)
Run any build / edit / decision / finding through three anchors; fail one -> it is "neat" or "massaging," not anchored, so stop or fix.
- **Anchor 1, VISION:** serves the destination, one true insight + one next action in the owner's currency (customers / money / time), truth over comfort, in-lane, runs without us.
- **Anchor 2, ICP (a real panel decides, not a generic persona):** Garrison (already #1 -> needs the NEXT move, not praise); Caroline / Artful (passive, won't log in -> must be PUSHED to her; a login screen fails her); Feras / 1Endo (needs data that is TRUE, their counter once showed 436 vs ~156 reality, and work that reflects what they asked for; "not technical, keep us in the loop").
- **Anchor 3, COMPETITIVE GAP:** be the partner, not another tool/dashboard; subtraction (one verdict) not more data; whole-journey not one slice. Moat = proprietary cross-journey signal + trust + partner category, NOT "we use AI" ("plain-English next step" is now table-stakes, Intuit Assist ships it to millions).

## 8. Source-of-truth hierarchy for the SEO / content standard (resolves the CroSEO-Mind vs repo vs Notion drift, 2026-07-08)
The SEO/content standard lived in THREE hand-maintained copies that drift: the **CroSEO Mind** (DB), the **repo seed migration**, and the **Notion "The Standard"** doctrine. Resolution (Corey delegated 2026-07-08), the source of truth is what EXECUTES:
- **Operational source of truth = the CroSEO Mind's published `brain_markdown`** (DB `minds.minds` / `mind_versions`, managed via `/admin/minds`). This is what the runtime actually reads (`service.seo-generation.ts` -> `fetchMindSkillCreator` / `fetchMindSkillValidator`), so it is what reaches the customer. A standard the code does not read is a wish, not a standard. All operational edits happen HERE, in one governed, versioned place.
- **Notion "The Standard, Runtime Principle Rubric" = the DOCTRINE** (the why + the acceptance bar), NOT a parallel operational copy. The Mind's `brain_markdown` must CONFORM to it. Edits flow ONE direction: doctrine (Notion) -> Mind (`/admin/minds`) -> runtime.
- **The repo seed migration (`20260225000003_seed_croseo_mind.ts`) = the INITIAL value only, NOT current truth.** After go-live the live DB Mind diverges from it; never treat the seed markdown as the current standard.
- **Drift-killer:** never maintain the standard in more than one editable place. The Mind is the one editable operational home; Notion documents the doctrine; the repo seed is historical. **Follow-up (owned with Dave, the Mind owner):** read the live Mind's `brain_markdown` and reconcile it against the Notion doctrine to see how far it drifted, then edit the Mind (not a third copy) to close the gap.

## 9. Decision authority, run on the mechanic, not on Corey (established 2026-07-08)
The bottleneck to speed is stopping to wait on Corey. Remove it. A decision escalates to Corey ONLY if it carries a blast radius that hits **customers or finances**, or **stakes canon**, or is **irreversible / outward** (the escalate-always set from the CoS-triage coherence pass, `strategy/cos-triage-agent.md`). Everything else: **proof and coherence decide, and the agent proceeds.** The triage safe-auto-handle boundary and this decision-authority boundary are ONE mechanic, so any Claude, or any person, is empowered to decide without stopping to ask. Corey holds intent + the few one-way doors (Blaber's mission-command: the commander holds the standard, the operator executes); the rigor holds the rest. Re-inserting Corey as the decision-gate on a no-blast-radius call is the exact bottleneck this removes. Sharpens the self-sufficiency gate + the Green/Yellow/Red operating-lanes: "run on principles and mechanics" is the operating system, not a slogan.
