# Connect end-to-end lever audit — raw findings (for #33 synthesis)
Captured 2026-07-08. 4 passes: conversion (in), discovery, Reflect, proven-lever menu (pending).

## CONVERSION-SIDE AUDIT (agent aab013f1) — the finish line is the WEAKEST, CONFIRMED
Alloro builds+hosts the site and captures the submission, but everything that turns a submission into a booked, attributed patient is missing.
- **Website:** BUILT + hosted (auto-gen from Places, `instantWebsiteGenerator.ts:482`). NO CRO/A-B testing (absent). Preview sites (*.sites.getalloro.com) have NO analytics tracker; instrumentation (Rybbit/Clarity/GSC) only AFTER a custom domain + manual. First impression built once, left UNMEASURED.
- **Form capture:** BUILT (`formSubmissionController.ts:426`, persists + emails practice). ⚠️ 13-step security pipeline mostly COMMENTED OUT (honeypot/timing/JS-challenge/origin/flood/dup disabled "for debugging"; rate limiter commented out). NO source/UTM/referrer captured (schema = project_id, form_name, contents, recipients, sender_ip, content_hash, is_flagged).
- ⭐ **SPEED-TO-LEAD: NOT BUILT (biggest hole).** The LEAD gets NOTHING (no auto-responder/SMS/thank-you); only the PRACTICE is emailed. Alloro MEASURES staleness (`oldest_unread_hours`, `sectionBuilders.ts:291`) but NEVER acts. 5-min window (21x qualify) entirely unhandled.
- **BOOKING: CONFIRMED ABSENT.** No Calendly/Acuity/NexHealth/Zocdoc/Reserve-with-Google. "Book Appointment" = hardcoded link to /contact; `htmlValidator.ts:438` FORCES all CTAs to /contact,tel,mailto ("do not invent /book-now"). PMS = read/ingest only, no appointment write-back. patient-journey = analytics funnel, not a booking surface.
- **ATTRIBUTION: UNBUILT for the customer journey.** Form stores NO source. `firstPatientAttribution.ts` = ORPHANED DEAD CODE (no caller, grep-confirmed). The ONLY instrumented source->conversion funnel = Alloro's OWN audit-signup (`LeadgenTrackingController`), a different journey, no bridge to a practice's visitors. No visitor->submission join, no submission->booking join (no booking entity exists).
- ⭐ SINGLE BIGGEST HOLE: speed-to-lead + attribution = same wound. At the moment the journey pays off, Alloro captures the lead but does NOTHING automated with it AND can't tell where it came from. Instant-response + booking-capture + source->booking attribution = where Delta precision is furthest from reality.
- CAVEAT: read code, not runtime; the page-HTML renderer is a SEPARATE service not in this repo (could theoretically inject trackers, but generateInstantWebsite provisions none).

## DISCOVERY-SIDE AUDIT (agent a5e7ef0d)
- **GBP:** ONLY 2 write paths hit Google = local POSTS (`gbp-write.service.ts:131`) + review REPLIES (`gbp-write.service.ts:81`), both human-gated. Everything else observe-or-advise.
  - Completeness: READ-ONLY (no write API to Google); CLIENT's own completeness never scored/tracked (only competitors', `util.competitor-profile-strength.ts`). Categories: read-only, generic advice, never applied. Photos: post-attach only, NO profile/gallery upload. NAP: NOT built on GBP side (unmeasured; LLM told to "consider nap_consistency" with no data).
  - Posts: built+deploy but scheduled auto-gen NEUTERED (skips locations w/ per_post_image_required; human-initiated). Reviews collection: solid (daily + velocity). Replies: built (human-approved); NO auto-draft for incoming; Apify-sourced reviews unreplyable.
  - Rank/map: built (SerpApi Maps, every 15 days). Single lat/lng, no geo-grid, sampled estimate.
- **GSC / Organic:**
  - Keyword research: LLM-INVENTED; DataForSEO removed 6/30 (commit fe9b038e, drop `20260630000000`). Zero volume/difficulty. Targets are guesses.
  - Web rank: Maps-only, NOT web SERP. On-page/content: built + published (DFT), strong but no volume informs WHAT to write. Schema/JSON-LD: built, deployed, fabrication-guarded = STRONGEST lever.
  - ⭐ GSC integration: BUILT + TRUE daily time series (queries/clicks/impressions/position, 16-mo backfill; `service.gsc-integration.ts:274`). BUT read-only, LOOP NOT CLOSED — real GSC query-DEMAND is NEVER fed into keyword/content selection. ⭐ Closing the GSC->content loop = highest-leverage fix, needs NO new integration (demand data already collected + FREE). Cheapest keyword-research fix, cheaper than rebuilding DataForSEO.
- ⭐⭐ **AEO/GEO: TOTAL BLIND SPOT. ZERO code** (grep: AEO=0, answer-engine=0, AI-Overview=0, perplexity=0, chatgpt=0, copilot=0, AI-visibility=0, tri-score=0). "Recognition Tri-Score (AEO)" = doc/plan only, zero code footprint. `ai-seo-audit/` does NOT query any AI engine, it scores the business's OWN pages via Google organic; no answer-engine feedback loop. The ONLY discovery lever with a total zero across built/monitored/DFT.
- ⭐ BIGGEST DISCOVERY HOLE: AEO/GEO absent, Alloro monitors ZERO of the AI-answer surfaces where a growing share of searches now happen (the "why now" wave). SECOND: the 6/30 removal left keyword-side with no demand data while the real GSC query signal sits UNUSED -> close the GSC->content loop (free, no new integration).

## REFLECT AUDIT (agent a6179841) — Reflect = the monthly-agents pipeline (Referral Engine -> Summary -> Opportunity -> CRO)
- ⭐ IMPORT-ONLY (verified): NO live PMS/EMR connector. 3 paths: file upload (`PmsController.ts:92`), manual entry (`:49`), paste (`:504`). dentrix/eaglesoft/nexhealth = ZERO code. "Open Dental" = just a CSV column-mapping template. Jo's "Dental EMR Parser" (Dave/Kuda) = an export PARSER, still the import path, NOT a connector.
- ⭐⭐ THE 2/3 PROBLEM CONFIRMED + WORSE: the monthly Reflect pipeline is triggered ONLY by a PMS import finalizing (`pms-finalize.service.ts:110`). Scheduled jobs = ONLY proofline + ranking, NOT the monthly pipeline. Other caller (/process-all) = DEPRECATED. So a NON-importer NEVER triggers the monthly pipeline (no Referral Engine / Summary / Opportunity / CRO). 2nd lock: even if entered, Referral Engine runs FIRST + aborts on failure (`service.monthly-agent-processor.ts:301`) before Summary/Opportunity/CRO ("no source = no ship"). Designed to refuse without PMS.
  - Nuance: non-importers DO get the separate PMS-INDEPENDENT layer (daily Proofline GBP/website Win-Risk, Practice Ranking, Surprise Findings, review insights). So not literally "nothing," but the monthly PATTERN-RECOGNITION / Reflect layer reaches 0 of non-importers. Founder's claim upheld.
- ⭐ REFERRAL-ONLY (verified): data model + signal = referrals only (self/doctor_referrals, `pmsAggregator.ts:48`; RE schema `doctor_referral_matrix`). Vocab templating re-labels WORDS, the SIGNAL is referrals. No non-referral value signal (revenue-by-service, repeat-visit, channel mix).
- ⭐⭐ THE CHEAP HIGH-LEVERAGE FIX (in-repo, reaches the 2/3 TODAY, NO new integration): DECOUPLE the monthly pipeline from PMS import. (1) trigger it on a SCHEDULE for ALL onboarded locations (not only on PMS finalize); (2) make Referral Engine a SKIPPABLE enrichment (guard `if(pmsDataForRE)`) so Summary/Opportunity/CRO run standalone instead of aborting at line 301.
- GAP A (import dependency): live connector (NexHealth API) = net-new, slow. GAP B (referral-only): define each business type's value signal + feed a non-referral rollup into the same aggregator; pipeline supports it, ingestion+signal don't exist.
- DECISIVE DATA CHECK: query agent_results on dev/prod for a never-imported org, if no agent_type='summary' rows -> "2/3 get no monthly Reflect" proven in data.

## PROVEN-LEVER MENU (agent a3a520dd) — key
9 players (Birdeye, Podium, Weave, Thryv, ServiceTitan, Owner, NiceJob, Signpost, Yext, Broadly).
- 🟥 CATEGORY-DEFINING: listings/citations; AEO/AI-visibility (ONLY Yext+Birdeye); review REQUESTS (ALL 9, most-proven lever in the market); webchat->web-to-text (Podium/Birdeye); speed-to-lead (Podium's whole company); online booking; two-way texting+inbox; AI receptionist/voice (market CONVERGED: Weave/ServiceTitan/Podium/Broadly/Signpost/Birdeye); call-tracking+attribution (ServiceTitan's moat).
- OWNER.COM META-LESSON: moat is NOT a feature, it's DONE-FOR-YOU (pre-built, pre-tested across thousands, run by a specialist on autopilot). Truest Alloro analog.
- Thinnest cluster = loyalty/win-back (only Owner strong) = an opening.

## ⭐⭐ SYNTHESIS — HAVE vs PROVEN (answer to "every lever, Delta precision?")
NO. Alloro competently does a SUBSET of the EXPECTED (🟧) levers, has ~ZERO of the CATEGORY-DEFINING (🟥).
- HAVE (🟧, DFT): schema (strongest), on-page content, review REPLIES + monitoring/collection, GBP posts, website build+host, Maps rank tracking.
- MISSING (🟥, what companies are BOUGHT for): speed-to-lead, booking, call-tracking/attribution, review REQUESTS, AEO, webchat-to-text, listings/citations, two-way inbox, AI receptionist.
- ⭐ THE DONE-FOR-YOU PROMISE IS AHEAD OF THE REALITY. Model right (Owner proves DFY = the moat); today covers the middle, misses both goalposts: discovery frontier (AEO/listings/real-keywords) + conversion finish (speed-to-lead/booking/attribution).
- RANKED IN-LANE GAPS (Attract->Appt): (1) speed-to-lead (biggest hole), (2) attribution/call-tracking (credibility layer + the map's Node 2 + "confident"), (3) booking (conversion endpoint), (4) review REQUESTS (most-proven, Corey's example, now Option-B-allowed, fastest), (5) AEO (WEDGE, only 2 players, the wave), (6) webchat-to-text + listings + real keyword research.
- ⭐ TWO NEAR-FREE UNLOCKS: (a) close the GSC->content loop (real demand already collected + free; cheapest keyword fix, no new integration); (b) decouple Reflect from PMS import (schedule the monthly pipeline for ALL locations + make referral-engine skippable -> reaches the 2/3 TODAY, in-repo, no connector).
- OPPORTUNITIES: AEO (uncontested wedge on the wave); review requests (most-validated, adjacent); loyalty/win-back (thinnest cluster, retention not Attract); Owner's done-for-you HOW is the differentiator IF the WHAT is covered.
- ROUTE IMPACT: confirms Phase 1 = the attribution/response rail = speed-to-lead + booking + attribution (finish + credibility). Add review-requests + AEO + the 2 free unlocks = close the biggest done-for-you holes.
