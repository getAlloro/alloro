SECTION: High Impact — Search Snippet & Click-Through (25 points)

Generate:
- "meta_description": Between 140-160 characters. Must include:
  1. A clear call-to-action ("Book today", "Call now", "Schedule a free consult")
  2. A trust signal (e.g. years in practice, review count, certifications) ONLY if it is directly supported by a VERIFIED PRACTICE FACTS entry or an explicit field in BUSINESS DATA — quote/reference the actual value, never invent a number or rating. If no such sourced fact exists, omit the trust-signal clause entirely and keep the CTA language only.
  Must be unique across all pages.
- "max_image_preview": Always set to "large".